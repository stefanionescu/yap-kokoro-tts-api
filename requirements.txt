# server
fastapi>=0.115.0,<0.116.0
uvicorn[standard]>=0.30.0,<0.31.0
python-dotenv==1.0.1
pydantic>=2.7.0,<3

# core
numpy==1.26.4
torch==2.7.1
requests>=2.31.0
websockets>=12.0
setuptools==69.1.1

# kokoro TTS
kokoro>=0.9.4
misaki[en]

# uvicorn extras (already included via [standard], kept for clarity)
httptools==0.6.1
tqdm==4.66.2
