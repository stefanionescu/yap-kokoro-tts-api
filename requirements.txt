# server
fastapi>=0.115.0,<0.116.0
uvicorn[standard]>=0.30.0,<0.31.0
python-dotenv==1.0.1
pydantic>=2.7.0,<3

# core stack
numpy==1.26.4
torch==2.7.1           # keep your current if it's stable on your node
transformers==4.55.1   # vLLM >=0.10 expects >=4.53
accelerate==0.28.0
pillow
timm==0.9.16

# vLLM + exact CT pin that vLLM 0.10.0 expects
vllm==0.10.0
compressed-tensors==0.10.2

# audio bits
snac==1.2.1
httptools==0.6.1
setuptools==69.1.1
tqdm==4.66.2
websockets==12.0
